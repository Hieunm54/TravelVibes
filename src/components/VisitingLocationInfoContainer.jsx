import React from "react";

const VisitingLocationInfoContainer = ({ children }) => {
  return (
    <div className="flex flex-col justify-between space-y-5">{children}</div>
  );
};

export default VisitingLocationInfoContainer;
